import styled from 'styled-components';
import { COLORS } from '~/consts/style';

export const Footer = styled.footer`
  padding: 20px 0;
  text-align: center;
  border-top: 1px solid ${COLORS.GRAY300.code};
  background-color: ${COLORS.GRAY100.code};
`;
