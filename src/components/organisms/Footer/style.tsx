import styled from 'styled-components';

export const Footer = styled.footer`
  padding: 20px 0;
  text-align: center;
  color: var(--nextui-colors-text);
  border-top: 1px solid var(--nextui-colors-border);
  background-color: var(--nextui-colors-background);
`;
